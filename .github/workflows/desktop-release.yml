name: Build Desktop Installers

on:
  push:
    tags: ["v*.*.*"]
  workflow_dispatch:

jobs:
  macos-arm64:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --no-frozen-lockfile
      - run: brew install ffmpeg
      - run: node FE/desktop-app/scripts/prepare-media-binaries.cjs
      - run: python3 -m pip install --user pyinstaller
      - run: node FE/desktop-app/scripts/build-voice-worker.cjs
      - run: pnpm --dir FE/desktop-app build
      - run: pnpm --dir FE/desktop-app exec electron-builder --mac dmg zip --arm64
      - uses: actions/upload-artifact@v4
        with:
          name: jacs-studio-macos-arm64
          path: FE/desktop-app/release/*

  macos-x64:
    # macos-13 is Intel, so the bundled FFmpeg is executable by the x64 app.
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --no-frozen-lockfile
      - run: brew install ffmpeg
      - name: Prepare Intel macOS media binaries
        run: node FE/desktop-app/scripts/prepare-media-binaries.cjs
        env:
          JACS_TARGET_ARCH: x64
      - run: python3 -m pip install --user pyinstaller
      - run: node FE/desktop-app/scripts/build-voice-worker.cjs
      - run: pnpm --dir FE/desktop-app build
      - run: pnpm --dir FE/desktop-app exec electron-builder --mac dmg zip --x64
      - uses: actions/upload-artifact@v4
        with:
          name: jacs-studio-macos-x64
          path: FE/desktop-app/release/*

  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --no-frozen-lockfile
      - run: choco install ffmpeg -y --no-progress
      - name: Prepare Windows media binaries
        run: node FE/desktop-app/scripts/prepare-media-binaries.cjs
        env:
          JACS_TARGET_ARCH: x64
      - run: python -m pip install pyinstaller
      - run: node FE/desktop-app/scripts/build-voice-worker.cjs
      - run: pnpm --dir FE/desktop-app build
      - run: pnpm --dir FE/desktop-app exec electron-builder --win nsis --x64
      - uses: actions/upload-artifact@v4
        with:
          name: jacs-studio-windows
          path: FE/desktop-app/release/*
